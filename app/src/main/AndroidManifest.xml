<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.usershop">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Myorderitemlist" />
        <activity android:name=".MyOrders" />
        <activity android:name=".Addaddress" />
        <activity android:name=".Cartoonpices" />
        <activity android:name=".Bagkg" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MyCart" />
        <activity android:name=".CartoonBoxPice" />
        <activity android:name=".OnlyCartoon" />
        <activity android:name=".CartoonLariPice" />
        <activity android:name=".OnlyBag" />
        <activity android:name=".SearchItem" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".OtpVerification"
            android:label=""
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>